__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/
dist/
*.egg-info/
.vscode/

# NOTE: data/seen_jobs.sqlite3 is intentionally NOT ignored.
# The GitHub Action commits it back to the repo after every run so that
# job history persists between runs (see .github/workflows/job-radar.yml).
